server:
  port: ":8081"
  worker_id: "worker1"
  technology: "go"

mysql:
  host: "localhost"
  port: 3306           # ← Use Computer 2's MySQL port
  user: "root"
  password: ""
  database: "worker1_db"

master:
  url: "http://192.168.1.100:8080"    # ← CHANGE to Computer 1's IP
  api_key: "my-secret-key-123"

reducer:
  url: "http://192.168.1.100:8090"    # ← CHANGE to Computer 1's IP

heartbeat_interval: 5